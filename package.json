{
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "name": "cls",
  "version": "1.0.0",
  "description": "in our hospital system, we have a patient management system to get patient data and resolve issues quickly. Using Flask and Peewee ORM.",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc --watch",
    "tail": "tailwindcss -i ./static/input.css -o ./static/output.css --watch"
  },
  "author": "Guilherme",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
